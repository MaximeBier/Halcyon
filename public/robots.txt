# Nothing is disallowed on purpose. The two application pages, capture.html
# and overlay.html, carry a `noindex` meta instead: a crawler forbidden to
# fetch a page never reads that instruction, and lists the bare URL anyway
# when a link points at it — and the README links to the capture page.
User-agent: *
Allow: /

Sitemap: https://halcyon.wardensquad.fr/sitemap.xml
